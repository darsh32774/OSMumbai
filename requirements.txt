psycopg2
tabulate
folium
python-dotenv
google-generativeai

# Notes:
# - PostgreSQL and PostGIS are system-level dependencies and must be installed separately.
# - Ensure the database has the extensions: postgis, hstore, pg_trgm.
# - osm2pgsql is required to import the .pbf file into PostgreSQL.
